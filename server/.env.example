PORT=3001
NODE_ENV=production
CORS_ORIGIN=https://yourdomain.com

DB_HOST=your-db-host
DB_PORT=5432
DB_NAME=grantfinder
DB_USER=postgres
DB_PASSWORD=your-db-password

# Set to any value to disable the Playwright CA scraper (required on hosts without browser binaries)
PLAYWRIGHT_SKIP=true
